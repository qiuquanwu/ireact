/*
*@ Ireact auto generate
*@ by qiuquanwu
*@ component-name ${name}
*/
import { atom } from "recoil";
const ${name} = atom({
    key: "${name}", // unique ID (with respect to other atoms/selectors)
    default: null, //array:[];null:null;string:""
});
export default ${name};
